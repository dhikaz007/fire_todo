/// Abstract interface for the Notifications feature repository.
///
/// This interface defines the contract for notification-related data operations.
abstract class INotificationRepository {
  /// Fetches the list of notifications for the current user.
  // Future<ResponseAPI<List<NotificationModel>>> fetchNotifications();
}
